This directory contains all static data in the database in the form of XML files.

searchdata.xml     - Contains all static data defining the search categories
                     and metadata types. It is loaded into the database by
                     the import_searchdata.pl script in the METAMODBASE module.

datasets/*.xml     - Contain the static datasets. These are datasets that are
                     originally created as XML files. They will be loaded
                     into the database (using the import_datacollection.pl 
                     script in the METAMODBASE module) when the database is
                     initialised.
