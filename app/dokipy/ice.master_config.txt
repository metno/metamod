TARGET = target2
BASE_DIRECTORY = /home/egils/egil
TARGET_DIRECTORY = [==BASE_DIRECTORY==]/dokipy/[==TARGET==]
SOURCE_DIRECTORY = [==BASE_DIRECTORY==]/m2/trunk
METAMODSEARCH_DIRECTORY = [==SOURCE_DIRECTORY==]/search
METAMODQUEST_DIRECTORY = [==SOURCE_DIRECTORY==]/quest

# ============ General configuration ==============
# Base part of external URL's. Concatenate this with the LOCAL_URL to get the
# full URL:
BASE_PART_OF_EXTERNAL_URL = http://ice.oslo.dnmi.no:8080

# Initial part of local URLs:
LOCAL_URL = /dokipy/[==TARGET==]/htdocs

# Application name:
APPLICATION_NAME = DOKIPY

# Application id (has to be short and unique among applications using the same
# database)::
APPLICATION_ID = DOKIPY

# Directory path for logfiles etc. generated by web applications:
WEBRUN_DIRECTORY = [==BASE_DIRECTORY==]/dokipy/webrun

# Hostname used for connecting to the PostgreSQL database:
PG_HOST =

# Port used for connecting to the PostgreSQL database:
PG_PORT =

# PG_CONNECTSTRING_* are strings used to indicate hostname and port to various
# commands and script functions. These strings should be empty if hostname and
# port are not used:
#
# String used in shell commands psql, createdb and dropdb to connect to the
# database:
PG_CONNECTSTRING_SHELL =

# String used in PHP db_Connect function to connect to the
# database:
PG_CONNECTSTRING_PHP =

# String used in Perl DBI->connect function to connect to the
# database:
PG_CONNECTSTRING_PERL =

# Name of the PostgreSQL database:
DATABASE_NAME = damocles

# URL of OPeNDAP server:
OPENDAP_URL = http://ice.oslo.dnmi.no:8081/thredds/catalog/data

# URL of Metadata Search application:
METADATA_SEARCH_URL = [==LOCAL_URL==]/sch/index.php

# URL of Upload application:
UPLOAD_URL = 

# Main menu beneath the application header. Each line should start with
# an URL followed by a space and some text. The text will be shown in
# the application as a menu item, and the URL will be activated when the
# text is clicked:
APP_MENU =
 http://dokipy.met.no/ home ...
 http://dokipy.met.no/database.html operational data overview
 [==METADATA_SEARCH_URL==] Search DOKIPY
 [==LOCAL_URL==]/qst/fmquest.php Operational data questionnaire
 [==LOCAL_URL==]/qst/fmmeta.php Metadata questionnaire
 [==OPENDAP_URL==] OPeNDAP

# HTML code for a header across the top of applications:
APP_HEADER_HTML =
 <a href="http://dokipy.met.no/"><img src="[==LOCAL_URL==]/img/ipy-logo.gif"
 style="clear:left;border: none;" alt="IPY logo"></a><br>
 <img src="[==LOCAL_URL==]/img/ipy_panorama_small.png" alt="IPY Panorama"
 style="width: 100%; border: solid thin;margin-top: 5px;">
 <br>

# HTML code for a footer across the bottom of applications:
APP_FOOTER_HTML =

# ============ METAMODSEARCH configuration ==============
# Name of web-based search application as seen by users:
SEARCH_APP_NAME = DOKIPY search

# Title to appear on all web pages in the web search application:
SEARCH_APP_TITLE = DOKIPY search

# HTML code for a header across the top of the search application:
SEARCH_APP_HEADER_HTML =
 <div class="myheader">
    <a href="http://www.ipy.org/"><img src="[==LOCAL_URL==]/img/ipy-logo.gif"
       style="clear:left;border: none;" alt="IPY logo"></a>
    <br>
    <img src="[==LOCAL_URL==]/img/ipy_panorama_small.png" style="border:solid
       thin;width:100%;vertical-align: middle;" alt="IPY Panorama image">
 </div>
 <div class="mystatusbar">
    <span class="mybreadcrumbs"><a
    href="http://dokipy.met.no/">DOKIPY</a>
    /search</span>
    <span class="myupdated">February&nbsp;28.&nbsp;2008</span>
    &nbsp;
 </div>

# Description of the web application:
SEARCH_APP_DESCRIPTION =
 Search for IPY data. Use the links on the left hand side
 to access pages for setting search conditions. Then use the menu below to
 view results.<BR /><BR />
 The catalogue contains datasets with references to actual data as well as
 metadata describing planned activities. Currently there are no direct
 connection from the planned activity descriptions to the corresponding
 datasets.

# Comma-separated list of dataset tags accessible from this application:
DATASET_TAGS = 'DAM', 'IPYC', 'DOKI'

# HTML to fill an initial page where no results are shown:
SEARCH_APP_PRESENTATION_HTML = <br />&nbsp;<br />

# Max number of columns allowed in the search result page:
SEARCH_APP_MAX_COLUMNS = 5

# Set of Metadata types to be shown as columns in search results.
# Each line below comprise at least two items (items with several words must be
# enclosed in apostophes). Item1 is MT_name in the Metadatatype table. Item2 is
# the column heading. One optional item is "col=n" where n is a number
# (1,2,...) designating the default column number of the item in the result table.
SEARCH_APP_SHOW_COLUMNS =
 dataref 'Name' col=1 cross=no
 area 'Areas' col=3 cross=v
 institution 'Institutions' col=2
 variable 'Topics and variables'
 activity_type 'Activity types' col=4 cross=h
 abstract 'Abstract' col=5 cross=no
 datacollection_period 'Datacollection period'
 contact 'Contact (E-mail)'
 title 'Descriptive title for the dataset'
 keywords 'Keywords' cross=no
 Product_name 'Product name'
 references 'References' cross=no
 field_type 'Field type (frequency)'
 forecast_type 'Forecast type'
 Platform_name 'Platform name'
 PI_name 'Principal investigator'
 distribution_statement 'Distribution statement'
 spatial_resolution 'Spatial resolution'
 operational_status 'Operational status'
 file_format 'File format'
 format_description 'Format description'
 metadata_standard 'Metadata standard'
 project_name 'Project name'

# The sequence by which the search categories are visible in the search
# application. The numbers are keys in the SearchCategory database table.
SEARCH_CATEGORY_SEQUENCE = 4,1,10,7,2,9,8

# Set default fontsize for the results and the two-way tables (percent):
DEFAULT_FONTSIZE = 80

# ============ METAMODQUEST configuration ==============
#
# Produce two different files from the same source file
# (htdocs/qst/quest.php). Each having its own values for the
# following macro variables:
#
#  QUEST_TITLE                Title for the questionnaire
#  QUEST_FORM_DEFINITON_FILE  File defining the content of the form
#  QUEST_OUTPUT_DIRECTORY     The directory (absolute path) where
#                             XML output files should be written.
#
!substitute_to_file_with_new_name htdocs/qst/quest.php => htdocs/qst/fmquest.php
QUEST_TITLE = DOKIPY data questionnaire
QUEST_FORM_DEFINITON_FILE = config/dokipy-config.txt
QUEST_OUTPUT_DIRECTORY = [==WEBRUN_DIRECTORY==]/XML/fmquest
!end_substitute_to_file_with_new_name

!substitute_to_file_with_new_name htdocs/qst/quest.php => htdocs/qst/fmmeta.php
QUEST_TITLE = DOKIPY meta data form
QUEST_FORM_DEFINITON_FILE = config/metamod2-meta-config.txt
QUEST_OUTPUT_DIRECTORY = [==WEBRUN_DIRECTORY==]/XML/[==APPLICATION_ID==]
!end_substitute_to_file_with_new_name

# Ownertag used to tag all dataset entries sent to the database:
QUEST_OWNERTAG = DOKI

# Sender address to appear on E-mail sent from this application:
QUEST_SENDER_ADDRESS = dokipy@met.no

# Comma-separated list of E-mail addresses for administrator personell.
# A summary of the filled in forms will be sent to these addresses:
# QUEST_RECIPIENTS = o.godoy@met.no
QUEST_RECIPIENTS = egil.storen@met.no

# Message shown to the user (as a web page) when the form has been submitted:
QUEST_OKMESSAGE = Thanks for submitting information! 
 <a href="[==LOCAL_URL==]/index.php">Return to the main page</a>.

# Background image for file administration page
QUEST_ADM_BACKGROUND = [==LOCAL_URL==]/img/ipydis_background.jpg

# Top directory for the file administration utility:
QUEST_ADM_TOPDIR = [==WEBRUN_DIRECTORY==]/XML

# ============ Miscellaneous ===============
# Set this value to a '#' to comment out using a locally installed Perl
# library directory:
USE_LOCAL_PERL_LIB =
